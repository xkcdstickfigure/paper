import Page from "../../components/Page";
import theme from "../../theme";

const userPage = props => (
	<Page
		title="@archie"
		breadcrumbs={[
			{
				name: "@archie"
			}
		]}
	></Page>
);

export default userPage;
